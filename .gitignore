# Editor / OS noise
.DS_Store
*~
*.swp
.vscode/
.idea/

# Claude Code per-session settings
.claude/

# Local scratch
.scratch/
*.tmp

# Fish state that shouldn't end up in a plugin repo
fish_variables
*.local.fish

# termtosvg intermediate output (none currently — single-step
# pipeline). SVGs in assets/ are tracked and committed.

# Stray pentest scan output that lands in the repo dir (e.g. running
# feroxbuster from the repo root). Never belongs here.
ferox-*.state
